import React from 'react';

const Users = () => {
    return(
        [
            {
                user: "admin",
                password: "admin"
            }
        ]
    )
}

export default Users;